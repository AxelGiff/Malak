services:
  db:
    image: postgres:latest
    environment:
      POSTGRES_USER: ${APPLICATION_NAME}_user
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "${DB_PORT}:5432"
    restart: always
